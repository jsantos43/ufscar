name: Autograding Tests
'on':
- push
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Test with behavioral check
      id: test-with-behavioral-check
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Test with behavioral check
        setup-command: "./setup.sh"
        command: "./tests/run.sh"
        timeout: 10
        max-score: 100
    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        TEST-WITH-BEHAVIORAL-CHECK_RESULTS: "${{steps.test-with-behavioral-check.outputs.result}}"
      with:
        runners: test-with-behavioral-check
